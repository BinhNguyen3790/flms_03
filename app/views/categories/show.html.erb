<%= provide(:title, @category.name) %>
<div class="container">
  <h1 class="my-4">
    <%= @category.name %><br/>
  </h1>
  <div class="row">

    <div class="col-md-4">
      <%= link_to image_tag("categories.jpg", alt: "Rails logo") %>
    </div>

    <div class="col-md-8">
      <%= render @category.books %>
    </div>
  </div>
</div>
