<% provide(:title, t(".category")) %>
<div class="row">
  <div class="col-sm-12">
    <div class="panel panel-warning">
      <div class="panel-heading">
        <i class="fa fa-table fa-fw"></i> <%= t(".category_table") %>
      </div>
      <div class="panel">
        <div class="row">
          <div class="col-md-2">
            <%= link_to t(".create"), new_admin_category_path,
              class: "btn btn-md btn-warning form-control" %>
          </div>
          <div class="col-md-2">
            <%= link_to t(".download"), admin_categories_path(format: "csv",
              search: params[:search]), class: "btn btn-md btn-success form-control" %>
          </div>
          <div class="col-md-8">
            <%= search_form_for @search, url: admin_categories_path do |f| %>
              <div class="active-cyan-4 mb-4">
                <%= f.search_field :name_cont, class: "form-control", placeholder: t(".search") %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
      <div class="table-responsive">
        <table class="table table-hover" id="example">
          <thead>
            <tr>
              <th><%= t ".image" %></th>
              <th><%= t ".name" %></th>
              <th colspan="2" class="center"><%= t ".action" %></th>
            </tr>
          </thead>
          <tbody>
            <%= render @categories %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<div class="row"><div class="text-center"><%= will_paginate %></div></div>
